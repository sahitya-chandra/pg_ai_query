{
  "sql": "SELECT * FROM users WHERE active = true ORDER BY created_at DESC LIMIT 100",
  "explanation": "This query retrieves all active users, sorted by creation date in descending order, limited to 100 results.",
  "warnings": ["Consider adding an index on the 'active' column for better performance"],
  "row_limit_applied": true,
  "suggested_visualization": "table"
}